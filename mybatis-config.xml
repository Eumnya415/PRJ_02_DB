<!--MyBatis 프레임워크를 구성하고 DB와의 연결을 설정.
이 MyBatis 설정 파일은 MyBatis 프레임워크에 DB 연결 및 트랜잭션 관리 정보를 제공하고,
"AddrMapper.xml"과 같은 매퍼 파일을 참조하여 SQL 쿼리를 실행하기 위한 설정을 포함한다.
이 설정 파일은 "development" 환경을 사용하여 DB와 상호작용하며, DB 연결 정보와 JDBC 트랜잭션 관리자를 정의한다.
-->

<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE configuration
  PUBLIC "-//mybatis.org//DTD Config 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-config.dtd">
<configuration> <!--MyBatis 설정을 포함-->
  <environments default="development">
    <environment id="development">
      <transactionManager type="JDBC"/>
      <dataSource type="POOLED">
        <property name="driver" value="org.mariadb.jdbc.Driver"/>
        <property name="url" value="jdbc:mariadb://127.0.0.1:3306/addr_prj"/>
        <property name="username" value="root"/>
        <property name="password" value="12345"/>
      </dataSource>
    </environment>
  </environments>
   
  <mappers>
    <mapper resource="AddrMapper.xml"/>
  </mappers>
   
</configuration>